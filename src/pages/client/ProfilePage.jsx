import React from "react";

const ProfilePage = () => {
	return (
		<div>
			<p>Xin chào, </p>
			<button onClick={() => setUser(null)}>Đăng xuất</button>
		</div>
	);
};

export default ProfilePage;
